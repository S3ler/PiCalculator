-module(pi).
-export([calculate/1].

%calculate(0) -> 
% spawn N time: PiElementActor = spawn(pi pi_element_actor:loop/0).
%calculate(N) when N > 0 -> N 


% spawn N time: PiElementActor = spawn(pi pi_element_actor:loop/0).
